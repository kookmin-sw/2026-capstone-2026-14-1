<div class="page active guest-index-page guest-scroll-container">
  <section class="guest-index-hero guest-scroll-section">
    <div class="guest-index-copy">
      <span class="guest-eyebrow">2026 KMU Capstone Design · Team 14</span>
      <h1>웹캠으로 시작하는<br><span>AI 운동 코칭</span></h1>
      <p>
        FitPlus는 별도 웨어러블 없이 브라우저 카메라만으로 사용자의 운동 자세를 분석하고,
        반복 횟수·운동 시간·자세 점수·개선 피드백을 실시간으로 제공하는 웹 기반 운동 코칭 서비스입니다.
      </p>
      <div class="guest-hero-actions">
        <a class="primary guest-action-primary" href="/login">로그인하고 시작하기</a>
        <a class="ghost guest-action-secondary" href="/signup">회원가입</a>
      </div>
      <div class="guest-metric-row" aria-label="프로젝트 핵심 지표">
        <div>
          <strong>3종</strong>
          <span>지원 운동</span>
        </div>
        <div>
          <strong>Live</strong>
          <span>실시간 자세 분석</span>
        </div>
        <div>
          <strong>Web</strong>
          <span>별도 장비 없음</span>
        </div>
      </div>
    </div>

    <div class="guest-phone-stage" aria-label="FitPlus 실시간 자세 분석 화면 예시">
      <div class="guest-glow-ring"></div>
      <div class="guest-dashboard-card">
        <div class="guest-mock-window">
          <div class="guest-mock-top">
            <span class="red"></span>
            <span class="yellow"></span>
            <span class="green"></span>
          </div>
          <div class="guest-camera-area">
            <svg class="guest-squat-stickman" viewBox="0 0 360 330" role="img" aria-label="측면에서 최대로 앉은 스쿼트 자세">
              <defs>
                <linearGradient id="guestStickmanStroke" x1="0" y1="0" x2="1" y2="1">
                  <stop offset="0%" stop-color="#38bdf8" />
                  <stop offset="100%" stop-color="#34d399" />
                </linearGradient>
              </defs>
              <line class="guest-stickman-guide" x1="70" y1="290" x2="300" y2="290" />
              <line class="guest-stickman-guide" x1="125" y1="198" x2="248" y2="198" />
              <g class="guest-stickman-squat">
                <path class="guest-stickman-line" d="M142 198 L158 158 L176 120 L185 96" />
                <path class="guest-stickman-line" d="M176 120 L240 106 L292 100" />
                <path class="guest-stickman-line" d="M174 128 L238 115 L290 109" />
                <path class="guest-stickman-line" d="M142 198 L232 194 L200 290" />
                <path class="guest-stickman-line" d="M168 290 L236 290" />
                <path class="guest-stickman-line" d="M142 198 L222 206 L188 290" />
                <path class="guest-stickman-line" d="M156 290 L224 290" />
                <circle class="guest-stickman-head" cx="192" cy="70" r="21" />
                <circle class="guest-stickman-joint" cx="185" cy="96" r="8" />
                <circle class="guest-stickman-joint" cx="176" cy="120" r="8" />
                <circle class="guest-stickman-joint" cx="240" cy="106" r="8" />
                <circle class="guest-stickman-joint" cx="292" cy="100" r="8" />
                <circle class="guest-stickman-joint" cx="158" cy="158" r="8" />
                <circle class="guest-stickman-joint" cx="142" cy="198" r="8" />
                <circle class="guest-stickman-joint" cx="232" cy="194" r="8" />
                <circle class="guest-stickman-joint" cx="222" cy="206" r="8" />
                <circle class="guest-stickman-joint" cx="200" cy="290" r="8" />
                <circle class="guest-stickman-joint" cx="188" cy="290" r="8" />
              </g>
            </svg>
            <div class="guest-score-chip">
              <span>FORM SCORE</span>
              <strong>92</strong>
            </div>
          </div>
          <div class="guest-feedback-panel">
            <div class="guest-feedback-row">
              <div><small>Squat</small><b>무릎 정렬 안정적</b></div>
              <div class="guest-progress"><span style="--w: 86%"></span></div>
            </div>
            <div class="guest-feedback-row">
              <div><small>Camera</small><b>측면 인식 양호</b></div>
              <div class="guest-progress"><span style="--w: 74%"></span></div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="guest-index-section guest-scroll-section">
    <div class="guest-section-head center">
      <span class="guest-kicker">WHY FITPLUS</span>
      <h2>혼자 운동할 때 가장 어려운 것은<br>“내 자세가 맞는지” 확인하는 일입니다.</h2>
      <p>
        홈트레이닝 환경에서는 트레이너의 즉각적인 피드백을 받기 어렵고, 사용자는 반복 횟수보다 더 중요한 자세 품질을 놓치기 쉽습니다.
        FitPlus는 이 문제를 웹캠 기반 실시간 분석으로 해결합니다.
      </p>
    </div>
    <div class="guest-problem-grid">
      <article class="guest-info-card">
        <div class="guest-icon-box">01</div>
        <h3>자세 오류 인지 부족</h3>
        <p>스쿼트 깊이, 무릎 정렬, 푸쉬업 상하 움직임처럼 사용자가 직접 확인하기 어려운 요소를 놓치기 쉽습니다.</p>
      </article>
      <article class="guest-info-card">
        <div class="guest-icon-box">02</div>
        <h3>운동 기록의 단절</h3>
        <p>운동을 수행해도 세트별 점수, 반복 수, 시간, 개선 흐름이 체계적으로 남지 않으면 성장 판단이 어렵습니다.</p>
      </article>
      <article class="guest-info-card">
        <div class="guest-icon-box">03</div>
        <h3>개인화 피드백 부족</h3>
        <p>단순한 운동 안내만으로는 사용자의 약점, 개선 추세, 다음 목표를 구체적으로 제시하기 어렵습니다.</p>
      </article>
    </div>
  </section>

  <section class="guest-index-section guest-solution-wrap guest-scroll-section">
    <div class="guest-panel guest-solution-copy">
      <span class="guest-kicker">SOLUTION</span>
      <h2>FitPlus는 카메라, 자세 추론, 운동별 규칙, 기록 분석을 하나의 코칭 흐름으로 연결합니다.</h2>
      <p>
        브라우저에서 MediaPipe Pose로 사용자의 관절 landmark를 추적하고, 운동별 모듈이 반복 횟수와 자세 품질을 판정합니다.
        서버는 운동 기록, 루틴, 퀘스트, 히스토리를 관리하여 단발성 운동을 지속 가능한 개선 과정으로 전환합니다.
      </p>
      <ul class="guest-check-list">
        <li>카메라 영상 원본은 서버에 저장하지 않고, 자세 추론은 클라이언트에서 수행</li>
        <li>스쿼트·푸쉬업·플랭크별 채점 기준을 분리하여 관리</li>
        <li>운동 결과와 히스토리를 기반으로 개선점과 다음 목표를 요약</li>
      </ul>
    </div>
    <div class="guest-panel guest-flow-board">
      <div class="guest-flow-item">
        <div>A</div>
        <span>
          <strong>Pose Tracking</strong>
          <small>웹캠 입력에서 신체 landmark를 추출합니다.</small>
        </span>
      </div>
      <div class="guest-flow-item">
        <div>B</div>
        <span>
          <strong>Exercise Logic</strong>
          <small>운동별 phase, rep, time, quality gate를 판정합니다.</small>
        </span>
      </div>
      <div class="guest-flow-item">
        <div>C</div>
        <span>
          <strong>Live Feedback</strong>
          <small>자세 점수와 수정 피드백을 운동 중 즉시 제공합니다.</small>
        </span>
      </div>
      <div class="guest-flow-item">
        <div>D</div>
        <span>
          <strong>Result & History</strong>
          <small>누적 기록을 바탕으로 사용자의 운동 수행 데이터를 분석합니다.</small>
        </span>
      </div>
    </div>
  </section>

  <section class="guest-index-section guest-scroll-section">
    <div class="guest-section-head">
      <span class="guest-kicker">CORE FEATURES</span>
      <h2>운동 수행부터 기록 분석까지<br>하나의 서비스로 제공합니다.</h2>
    </div>
    <div class="guest-feature-grid">
      <article class="guest-feature-card large">
        <div class="guest-icon-box">↗</div>
        <h3>실시간 자세 분석 및 점수화</h3>
        <p>
          MediaPipe Pose 기반 landmark를 이용해 관절 각도, 자세 안정성, 반복 수행 상태를 분석하고,
          운동별 기준에 따라 자세 점수와 피드백을 제공합니다.
        </p>
        <div class="guest-exercise-tags">
          <span>Squat · FRONT / SIDE / DIAGONAL</span>
          <span>Push-up · SIDE</span>
          <span>Plank · SIDE</span>
        </div>
      </article>
      <article class="guest-feature-card">
        <div class="guest-icon-box">Ⅱ</div>
        <h3>자유 운동</h3>
        <p>사용자가 원하는 운동을 선택해 즉시 실시간 코칭 세션을 시작합니다.</p>
      </article>
      <article class="guest-feature-card">
        <div class="guest-icon-box">Ⅲ</div>
        <h3>운동 배우기</h3>
        <p>운동별 단계형 가이드와 코칭 흐름을 통해 자세를 학습할 수 있습니다.</p>
      </article>
      <article class="guest-feature-card">
        <div class="guest-icon-box">Ⅳ</div>
        <h3>루틴 운동</h3>
        <p>여러 운동을 묶어 루틴을 만들고 단계별 운동 세션으로 실행합니다.</p>
      </article>
      <article class="guest-feature-card">
        <div class="guest-icon-box">Ⅴ</div>
        <h3>히스토리 분석</h3>
        <p>과거 운동 기록, 세션 상세, 통계, 개선 포인트를 확인합니다.</p>
      </article>
      <article class="guest-feature-card">
        <div class="guest-icon-box">★</div>
        <h3>성취도 및 퀘스트</h3>
        <p>운동 출석, 누적 기록 등에 따른 목표 달성도와 뱃지를 통해 지속적인 동기를 부여합니다.</p>
      </article>
    </div>
  </section>

  <section class="guest-index-section guest-scroll-section">
    <div class="guest-section-head center">
      <span class="guest-kicker">USER FLOW</span>
      <h2>사용자는 복잡한 장비 없이<br>브라우저에서 바로 운동을 시작합니다.</h2>
    </div>
    <div class="guest-process-graph">
      <article>
        <span>01</span>
        <h3>운동 선택</h3>
        <p>자유 운동, 배우기, 루틴 중 목적에 맞는 운동 흐름을 선택합니다.</p>
      </article>
      <article>
        <span>02</span>
        <h3>카메라 준비</h3>
        <p>웹캠 권한을 허용하고 운동별 권장 시점에 맞춰 자세를 잡습니다.</p>
      </article>
      <article>
        <span>03</span>
        <h3>실시간 코칭</h3>
        <p>반복 횟수, 운동 시간, 자세 점수, 음성 피드백을 운동 중 확인합니다.</p>
      </article>
      <article>
        <span>04</span>
        <h3>결과 분석</h3>
        <p>세션 결과와 히스토리를 저장하고 기록을 통해 성장을 확인합니다.</p>
      </article>
    </div>
  </section>

  <section class="guest-index-section guest-scroll-section">
    <div class="guest-section-head">
      <span class="guest-kicker">RELIABILITY</span>
      <h2>점수와 피드백을 신뢰할 수 있도록<br>검증 가능한 기준을 둡니다.</h2>
      <p>운동 보조 서비스에서 중요한 것은 단순히 점수를 보여주는 것이 아니라, 어떤 조건에서 분석이 유효한지 설명하는 것입니다.</p>
    </div>
    <div class="guest-trust-grid">
      <article>
        <h3>분석 품질 관리</h3>
        <p>
          landmark confidence, 카메라 시점, 자세 인식 안정성에 따라 잘못된 점수를 내기보다 HOLD 또는 안내를 우선합니다.
          이를 통해 낮은 품질의 입력에서 과도하게 확정적인 피드백을 제공하지 않도록 설계합니다.
        </p>
      </article>
      <article>
        <h3>운동별 규칙 분리</h3>
        <p>
          스쿼트, 푸쉬업, 플랭크는 서로 다른 신체 움직임을 평가하므로 각 운동 모듈에 phase, rep/time, score, feedback 기준을 분리합니다.
          공통 엔진과 운동별 규칙을 나누어 유지보수성과 검증 가능성을 높입니다.
        </p>
      </article>
      <article>
        <h3>개인정보 보호</h3>
        <p>
          카메라 영상 원본은 서버에 저장하지 않습니다. 자세 추론은 브라우저에서 수행하고, 서버에는 운동 결과와 요약 metric 중심의 기록을 저장합니다.
        </p>
      </article>
      <article>
        <h3>서비스 한계 명시</h3>
        <p>
          FitPlus는 운동 자세 개선을 돕는 보조 서비스이며 의료 진단, 치료, 재활 처방을 대체하지 않습니다.
          사용자는 피드백을 참고하되 통증이나 부상 위험이 있으면 전문가의 지도를 받아야 합니다.
        </p>
      </article>
    </div>
  </section>

  <section class="guest-login-band guest-scroll-section">
    <div>
      <span class="guest-kicker">START FITPLUS</span>
      <h2>로그인하면 루틴, 퀘스트, 히스토리까지 이어서 사용할 수 있습니다.</h2>
    </div>
    <div class="guest-hero-actions">
      <a class="primary guest-action-primary" href="/login">로그인</a>
      <a class="ghost guest-action-secondary" href="/signup">회원가입</a>
    </div>
  </section>
</div>

<style>
  /* ── Scroll-snap: one section per scroll ── */
  .guest-scroll-container {
    gap: 0 !important;
    overflow-y: auto;
    scroll-snap-type: y mandatory;
    scroll-behavior: smooth;
    height: 100vh;
    height: 100svh;
    margin: -32px;
    padding: 0;
  }
  .guest-scroll-section {
    scroll-snap-align: start;
    min-height: 100vh;
    min-height: 100svh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: clamp(32px, 5vw, 64px) clamp(24px, 4vw, 56px);
    box-sizing: border-box;
  }
  .guest-scroll-section.guest-index-hero {
    flex-direction: row;
    align-items: center;
    gap: 28px;
  }

  /* ── Fade-in on scroll ── */
  .guest-fade-in {
    opacity: 0;
    transform: translateY(32px);
    transition: opacity 0.7s ease, transform 0.7s ease;
  }
  .guest-fade-in.guest-visible {
    opacity: 1;
    transform: translateY(0);
  }

  /* ── Hero Animations (from index.html) ── */
  @keyframes guestSquatMotion {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(12px); }
  }
  @keyframes guestScanline {
    0% { transform: translateY(-50px); opacity: 0; }
    50% { opacity: 0.6; }
    100% { transform: translateY(370px); opacity: 0; }
  }
  @keyframes guestLift {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-14px); }
  }
  @keyframes guestGlowSpin {
    to { transform: rotate(360deg); }
  }
  @keyframes guestPulseScore {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.06); }
  }
  @keyframes guestFloat {
    0%, 100% { transform: translate3d(0,0,0) scale(1); }
    50% { transform: translate3d(0,-20px,0) scale(1.03); }
  }
  .guest-stickman-squat {
    animation: guestSquatMotion 3s ease-in-out infinite;
  }
  .guest-camera-area::after {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 2px;
    background: #38bdf8;
    box-shadow: 0 0 20px #38bdf8;
    animation: guestScanline 3s linear infinite;
    z-index: 10;
  }
  .guest-dashboard-card {
    animation: guestLift 5.5s ease-in-out infinite;
  }
  .guest-glow-ring {
    position: absolute;
    width: 480px;
    height: 480px;
    border-radius: 999px;
    background:
      radial-gradient(circle, rgba(56,189,248,0.18), transparent 60%),
      conic-gradient(from 210deg, rgba(56,189,248,0.22), rgba(52,211,153,0.24), rgba(99,102,241,0.2), rgba(56,189,248,0.22));
    animation: guestGlowSpin 16s linear infinite;
    pointer-events: none;
    z-index: 0;
  }
  .guest-score-chip {
    animation: guestPulseScore 3s ease-in-out infinite;
  }

  .guest-index-page {
    gap: 0;
  }

  .guest-index-hero {
    position: relative;
    display: grid;
    grid-template-columns: minmax(0, 1.02fr) minmax(340px, 0.98fr);
    gap: 28px;
    align-items: center;
    overflow: hidden;
    border-radius: 0;
    padding: clamp(28px, 5vw, 56px);
    background:
      radial-gradient(circle at 14% 18%, rgba(56, 189, 248, 0.22), transparent 28%),
      radial-gradient(circle at 84% 12%, rgba(52, 211, 153, 0.24), transparent 30%),
      radial-gradient(circle at 50% 80%, rgba(99, 102, 241, 0.14), transparent 40%),
      linear-gradient(135deg, #111827 0%, #172554 48%, #0f766e 100%);
    color: #ffffff;
    box-shadow: 0 28px 80px rgba(15, 23, 42, 0.32);
  }

  .guest-index-copy {
    display: flex;
    flex-direction: column;
    gap: 22px;
    min-width: 0;
  }

  .guest-eyebrow,
  .guest-kicker {
    font-size: 12px;
    font-weight: 800;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .guest-eyebrow {
    color: #67e8f9;
  }

  .guest-kicker {
    color: var(--primary);
  }

  .guest-index-hero h1 {
    margin: 0;
    font-size: clamp(42px, 6vw, 76px);
    line-height: 1.02;
    letter-spacing: 0;
  }

  .guest-index-hero h1 span {
    color: #86efac;
  }

  .guest-index-hero p,
  .guest-section-head p,
  .guest-info-card p,
  .guest-solution-copy p,
  .guest-check-list,
  .guest-flow-item small,
  .guest-feature-card p,
  .guest-process-graph p,
  .guest-trust-grid p {
    margin: 0;
    line-height: 1.7;
  }

  .guest-index-hero p {
    max-width: 700px;
    color: rgba(255, 255, 255, 0.78);
    font-size: 16px;
  }

  .guest-hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    align-items: center;
  }

  .guest-action-primary,
  .guest-action-secondary {
    min-height: 44px;
    font-weight: 800;
  }

  .guest-index-hero .guest-action-secondary {
    background: rgba(255, 255, 255, 0.12);
    color: #ffffff;
    border: 1px solid rgba(255, 255, 255, 0.2);
  }

  .guest-metric-row {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 12px;
    max-width: 560px;
  }

  .guest-metric-row div {
    border: 1px solid rgba(255, 255, 255, 0.16);
    border-radius: 18px;
    padding: 14px;
    background: rgba(255, 255, 255, 0.08);
    backdrop-filter: blur(16px);
  }

  .guest-metric-row strong,
  .guest-metric-row span {
    display: block;
  }

  .guest-metric-row strong {
    font-size: 24px;
  }

  .guest-metric-row span {
    margin-top: 4px;
    color: rgba(255, 255, 255, 0.7);
    font-size: 12px;
    font-weight: 700;
  }

  .guest-phone-stage {
    position: relative;
    min-width: 0;
    min-height: 480px;
    display: grid;
    place-items: center;
  }

  .guest-dashboard-card {
    position: relative;
    z-index: 1;
    width: min(100%, 470px);
    margin-left: auto;
    border-radius: 30px;
    padding: 14px;
    background: linear-gradient(145deg, rgba(255, 255, 255, 0.24), rgba(255, 255, 255, 0.08));
    border: 1px solid rgba(255, 255, 255, 0.24);
    box-shadow: 0 32px 70px rgba(0, 0, 0, 0.32);
    backdrop-filter: blur(18px);
  }

  .guest-mock-window {
    overflow: hidden;
    border-radius: 24px;
    background: #0f172a;
    border: 1px solid rgba(255, 255, 255, 0.12);
  }

  .guest-mock-top {
    height: 40px;
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 0 16px;
    background: #111827;
  }

  .guest-mock-top span {
    width: 10px;
    height: 10px;
    border-radius: 50%;
  }

  .guest-mock-top .red { background: #fb7185; }
  .guest-mock-top .yellow { background: #facc15; }
  .guest-mock-top .green { background: #34d399; }

  .guest-camera-area {
    position: relative;
    min-height: 350px;
    display: grid;
    place-items: center;
    overflow: hidden;
    background:
      linear-gradient(180deg, rgba(15,23,42,0.1), rgba(15,23,42,0.9)),
      radial-gradient(circle at 50% 28%, rgba(56,189,248,0.22), transparent 35%),
      linear-gradient(rgba(255, 255, 255, 0.06) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255, 255, 255, 0.06) 1px, transparent 1px),
      linear-gradient(150deg, #111827 0%, #1e293b 54%, #155e75 100%);
    background-size: auto, auto, 32px 32px, 32px 32px, auto;
  }

  .guest-squat-stickman {
    width: min(88%, 360px);
    height: auto;
  }

  .guest-stickman-guide {
    stroke: rgba(255, 255, 255, 0.2);
    stroke-width: 2;
    stroke-dasharray: 8 8;
  }

  .guest-stickman-line {
    fill: none;
    stroke: url(#guestStickmanStroke);
    stroke-width: 14;
    stroke-linecap: round;
    stroke-linejoin: round;
    filter: drop-shadow(0 0 12px rgba(56, 189, 248, 0.42));
  }

  .guest-stickman-head {
    fill: rgba(15, 23, 42, 0.92);
    stroke: url(#guestStickmanStroke);
    stroke-width: 10;
  }

  .guest-stickman-joint {
    fill: #e0f2fe;
    stroke: #0f172a;
    stroke-width: 3;
  }

  .guest-score-chip {
    position: absolute;
    right: 16px;
    top: 16px;
    min-width: 98px;
    border-radius: 16px;
    padding: 12px;
    background: rgba(255, 255, 255, 0.94);
    color: #0f172a;
  }

  .guest-score-chip span {
    display: block;
    color: #64748b;
    font-size: 11px;
    font-weight: 800;
  }

  .guest-score-chip strong {
    display: block;
    margin-top: 2px;
    font-size: 34px;
    line-height: 1;
  }

  .guest-feedback-panel {
    display: grid;
    gap: 12px;
    padding: 16px;
    background: #111827;
  }

  .guest-feedback-row {
    display: grid;
    grid-template-columns: minmax(0, 1fr) 120px;
    gap: 12px;
    align-items: center;
  }

  .guest-feedback-row small,
  .guest-feedback-row b {
    display: block;
  }

  .guest-feedback-row small {
    color: #94a3b8;
    font-size: 11px;
  }

  .guest-feedback-row b {
    color: #ffffff;
    font-size: 13px;
  }

  .guest-progress {
    height: 8px;
    overflow: hidden;
    border-radius: 999px;
    background: rgba(148, 163, 184, 0.26);
  }

  .guest-progress span {
    display: block;
    width: var(--w);
    height: 100%;
    border-radius: inherit;
    background: linear-gradient(90deg, #38bdf8, #34d399);
  }

  .guest-index-section {
    display: grid;
    gap: 32px;
    align-content: center;
  }

  .guest-section-head {
    display: flex;
    flex-direction: column;
    gap: 10px;
    max-width: 860px;
  }

  .guest-section-head.center {
    margin-inline: auto;
    text-align: center;
    align-items: center;
  }

  .guest-section-head h2,
  .guest-solution-copy h2,
  .guest-login-band h2 {
    margin: 0;
    color: var(--text);
    font-size: clamp(26px, 3.4vw, 42px);
    line-height: 1.22;
    letter-spacing: 0;
  }

  .guest-section-head p,
  .guest-info-card p,
  .guest-solution-copy p,
  .guest-check-list,
  .guest-flow-item small,
  .guest-feature-card p,
  .guest-process-graph p,
  .guest-trust-grid p {
    color: var(--muted);
  }

  .guest-problem-grid,
  .guest-feature-grid,
  .guest-trust-grid {
    display: grid;
    gap: 18px;
  }

  .guest-problem-grid {
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }

  .guest-info-card,
  .guest-feature-card,
  .guest-trust-grid article,
  .guest-panel,
  .guest-process-graph article,
  .guest-login-band {
    border: 1px solid var(--border);
    background: var(--card);
    box-shadow: var(--shadow);
  }

  .guest-info-card,
  .guest-feature-card,
  .guest-trust-grid article,
  .guest-process-graph article {
    border-radius: 20px;
    padding: 28px;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .guest-info-card:hover,
  .guest-feature-card:hover,
  .guest-trust-grid article:hover,
  .guest-process-graph article:hover {
    transform: translateY(-4px);
    box-shadow: 0 20px 50px rgba(15, 23, 42, 0.14);
  }

  .guest-info-card,
  .guest-feature-card {
    display: flex;
    flex-direction: column;
    gap: 14px;
  }

  .guest-icon-box {
    width: 48px;
    height: 48px;
    border-radius: 16px;
    display: grid;
    place-items: center;
    background: linear-gradient(135deg, var(--primary), var(--accent));
    color: #fff;
    font-size: 14px;
    font-weight: 900;
    box-shadow: 0 12px 28px rgba(91, 108, 255, 0.22);
  }

  .guest-info-card h3,
  .guest-feature-card h3,
  .guest-process-graph h3,
  .guest-trust-grid h3 {
    margin: 0;
    color: var(--text);
    font-size: 19px;
  }

  .guest-solution-wrap {
    grid-template-columns: minmax(0, 1.02fr) minmax(0, 0.98fr);
    align-items: stretch;
    align-content: center;
  }

  .guest-panel {
    border-radius: 22px;
    padding: 24px;
  }

  .guest-solution-copy {
    display: flex;
    flex-direction: column;
    gap: 16px;
  }

  .guest-check-list {
    display: grid;
    gap: 10px;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .guest-check-list li {
    position: relative;
    padding-left: 24px;
  }

  .guest-check-list li::before {
    content: "";
    position: absolute;
    left: 0;
    top: 0.72em;
    width: 9px;
    height: 9px;
    border-radius: 50%;
    background: var(--accent);
  }

  .guest-flow-board {
    display: grid;
    gap: 12px;
  }

  .guest-flow-item {
    display: grid;
    grid-template-columns: 46px 1fr;
    gap: 12px;
    align-items: center;
    padding: 14px;
    border-radius: 16px;
    background: var(--input-bg);
  }

  .guest-flow-item div {
    width: 46px;
    height: 46px;
    border-radius: 14px;
    display: grid;
    place-items: center;
    background: #0f172a;
    color: #ffffff;
    font-weight: 800;
  }

  .guest-flow-item strong,
  .guest-flow-item small {
    display: block;
  }

  .guest-flow-item strong {
    color: var(--text);
  }

  .guest-feature-grid {
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }

  .guest-feature-card.large {
    grid-column: span 2;
  }

  .guest-exercise-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin-top: auto;
  }

  .guest-exercise-tags span {
    border-radius: 999px;
    padding: 7px 10px;
    background: var(--tab-bg);
    color: var(--ghost-color);
    font-size: 12px;
    font-weight: 700;
  }

  .guest-process-graph {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 16px;
  }

  .guest-process-graph article {
    position: relative;
    min-height: 190px;
    display: flex;
    flex-direction: column;
    gap: 10px;
  }

  .guest-process-graph span {
    width: 44px;
    height: 44px;
    border-radius: 50%;
    display: grid;
    place-items: center;
    background: linear-gradient(135deg, var(--primary), var(--accent));
    color: #ffffff;
    font-size: 12px;
    font-weight: 800;
  }

  .guest-trust-grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .guest-trust-grid article {
    display: grid;
    gap: 10px;
    box-shadow: none;
  }

  .guest-login-band {
    border-radius: 28px;
    padding: 48px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 28px;
    text-align: center;
    background:
      radial-gradient(circle at 14% 18%, rgba(56, 189, 248, 0.18), transparent 28%),
      radial-gradient(circle at 84% 72%, rgba(52, 211, 153, 0.2), transparent 30%),
      linear-gradient(135deg, #111827 0%, #172554 48%, #0f766e 100%);
    color: #ffffff;
    box-shadow: 0 28px 80px rgba(15, 23, 42, 0.24);
    border: none;
  }

  .guest-login-band h2 {
    color: #ffffff;
  }

  .guest-login-band .guest-kicker {
    color: #67e8f9;
  }

  .guest-login-band .guest-action-secondary {
    background: rgba(255, 255, 255, 0.12);
    color: #ffffff;
    border: 1px solid rgba(255, 255, 255, 0.2);
  }

  .guest-login-band > div:first-child {
    display: grid;
    gap: 12px;
  }

  [data-theme="dark"] .guest-flow-item div {
    background: #e2e8f0;
    color: #0f172a;
  }

  @media (max-width: 1080px) {
    .guest-scroll-container {
      scroll-snap-type: none;
      height: auto;
    }
    .guest-scroll-section {
      min-height: auto;
      scroll-snap-align: none;
    }

    .guest-index-hero,
    .guest-solution-wrap {
      grid-template-columns: 1fr;
    }

    .guest-dashboard-card {
      margin-inline: auto;
    }

    .guest-feature-grid,
    .guest-problem-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .guest-process-graph {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (max-width: 700px) {
    .guest-index-page {
      gap: 28px;
    }

    .guest-index-hero {
      min-height: 0;
      border-radius: 0;
      padding: 24px;
    }

    .guest-index-hero h1 {
      font-size: 38px;
    }

    .guest-hero-actions,
    .guest-login-band {
      align-items: stretch;
      flex-direction: column;
    }

    .guest-hero-actions a {
      width: 100%;
    }

    .guest-metric-row,
    .guest-feature-grid,
    .guest-problem-grid,
    .guest-process-graph,
    .guest-trust-grid {
      grid-template-columns: 1fr;
    }

    .guest-feature-card.large {
      grid-column: auto;
    }

    .guest-camera-area {
      min-height: 300px;
    }

    .guest-feedback-row {
      grid-template-columns: 1fr;
    }

    .guest-login-band {
      border-radius: 18px;
      padding: 22px;
    }
  }
</style>

<script>
(function initGuestAnimations() {
  // Fade-in observer for sections and cards
  const fadeEls = document.querySelectorAll(
    '.guest-scroll-section .guest-section-head,' +
    '.guest-scroll-section .guest-info-card,' +
    '.guest-scroll-section .guest-feature-card,' +
    '.guest-scroll-section .guest-panel,' +
    '.guest-scroll-section .guest-process-graph article,' +
    '.guest-scroll-section .guest-trust-grid article,' +
    '.guest-scroll-section .guest-problem-grid,' +
    '.guest-scroll-section .guest-feature-grid,' +
    '.guest-scroll-section .guest-flow-board,' +
    '.guest-login-band > div'
  );

  fadeEls.forEach(function(el) {
    el.classList.add('guest-fade-in');
  });

  var scrollRoot = document.querySelector('.guest-scroll-container');
  var fadeObserver = new IntersectionObserver(function(entries) {
    entries.forEach(function(entry) {
      if (entry.isIntersecting) {
        entry.target.classList.add('guest-visible');
      }
    });
  }, { threshold: 0.15, root: scrollRoot.scrollHeight > scrollRoot.clientHeight ? scrollRoot : null });

  fadeEls.forEach(function(el, i) {
    el.style.transitionDelay = Math.min(i % 4 * 80, 240) + 'ms';
    fadeObserver.observe(el);
  });

  // Progress bar animation
  var progressBars = document.querySelectorAll('.guest-progress span');
  var progObserver = new IntersectionObserver(function(entries) {
    entries.forEach(function(entry) {
      if (entry.isIntersecting) {
        entry.target.style.transform = 'scaleX(1)';
      }
    });
  }, { threshold: 0.5 });
  progressBars.forEach(function(bar) {
    bar.style.transformOrigin = 'left';
    bar.style.transform = 'scaleX(0)';
    bar.style.transition = 'transform 1.5s cubic-bezier(0.2,0.8,0.2,1) 0.8s';
    progObserver.observe(bar);
  });
})();
</script>
